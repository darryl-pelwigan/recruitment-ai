# Recruitment AI - Application Map

## Public Pages

- Login (`/login`)
- Register (`/register`)
- Jobs (`/jobs`) — guests can browse and view job details, but not apply or see salary
- Job Detail (`/jobs/:id`)

---

## Dashboard

- Dashboard (`/dashboard`)

---

## Jobs (Recruiter)

- Post Job (`/jobs/new`)
- Edit Job (`/jobs/:id/edit`)
- Applicants for a Job (`/jobs/:id/applicants`)
- Pipeline (`/jobs/:id/pipeline`)

---

## Applicants (Recruiter)

- All Applicants (`/applicants`)
- Applicant Profile (`/applicants/:id`)
- Saved Applicants (`/saved-applicants`)

---

## Applicant (Job Seeker)

- My Applications (`/my-applications`)
- Saved Jobs (`/saved-jobs`)
- Resumes (`/resumes`)

---

## Account

- Profile (`/profile`)

---

## Administration

- User Management (`/admin/users`) — requires the `admin` role
